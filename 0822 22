#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int a,b;
	while(cin>>a>>b)
	{
		if(a==0&&b==0)
		{
			break;
		}
		int u=0;
		a=ceil(sqrt(a));
		b=floor(sqrt(b));
		for(int i=a;i<=b ;i++)
		{
			u++;
		}
		cout<<u<<endl;
	}
}