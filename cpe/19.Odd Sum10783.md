```c
#include <iostream>
using namespace std;
int main()
{
	int t, a, b;
	cin >> t;
	int o = 1;
	int s;
	while (t--)
	{
		cin >> a >> b;
		s = 0;
		for (int i = a; i <= b; i++)
		{
			if (i % 2 == 1)
			{
				s = s + i;
			}
		}
		cout << "Case " << o << ": " << s << endl;
		o++;
	}
}
```
