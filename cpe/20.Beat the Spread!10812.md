```c
#include<iostream>
using namespace std;
int main()
{
	int t, s, d;
	int a, b;
	cin >> t;
	while (t--)
	{
		cin >> s >> d;
		if (s < d || (s+d)%2==1)
		{
			cout << "impossible" << endl;
			continue;
		}
		a = (s + d) / 2;
		b = s - a;
		cout << a << " " << b << endl;
	}
}
```
